<%#
<% @visit.photos.each do |photo| %>
  <%# <%= image_tag(photo.variant(resize_to_limit: [300, 300])) %>
<%# <% end %>

<% @visit.photos.each do |photo| %>
  <%= MiniMagick::Image.open(photo) %>
<% end %>
